# IdeaForge — Flow Execution Configuration
# Place this at: root/agent-config.yml

# This file configures how the IdeaForge flow runs in GitLab CI/CD.
# The runner must have the gitlab--duo tag and be instance-level or top-level group.

# Optional: Add setup scripts that run before the flow executes
# setup_script:
#   - echo "IdeaForge pipeline starting..."
